extends layout

block content
    header
        h1= "Temporary results"
    each result in results
        p= result.averageRGB
        p= result.size
        p= result.format
        p= result.url
        div(style="width: 50px; height: 50px; background-color: rgb(" + result.averageRGB + ");" )
        br